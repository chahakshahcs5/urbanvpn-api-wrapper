// TODO: add more client apps?
/**
 * client app string.
 * very useless right now since
 * we don't know any other client app types
 * than the browser extension's one
 * */
export type ClientApp =
	| "URBAN_SHIELD_BROWSER_EXTENSION"
	| string;
